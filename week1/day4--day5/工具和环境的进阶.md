学习使用pip的包管理和虚拟环境的使用
（了解了uv等现代包管理工具）
1.pip
	pip freeze > requirements.txt 
	pip install -r requirements.txt 
	但是这里生成requirements.txt后，这里面有直接依赖和很多间接依赖的包，所以很难管理，于是就有了project.toml这个文件来处理和核心的包
2.虚拟环境
•	创建环境python -m venv venv
    激活环境 ctrl+shift+p



学习使用git，github 

    
学习使用git与cc的基础用法


将项目传到github，将GitHub的仓库克隆到本地