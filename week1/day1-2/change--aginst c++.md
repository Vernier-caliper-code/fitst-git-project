1.
for z in  range(5,10):
    print(z)
注意这里的z是临时变量，但是这个循环结束之后，还是可以访问z这个变量的，
也就是说Python没有块级作用域（block scope）
------------------------------------------------------------------------------------------------------------------

2.
a=in
a=int(a)  #注意这里一定要用一个变量来接受，int(a)不修改原变量的值
print(a)
------------------------------------------------------------------------------------------------------------------

3.None的用法
作为函数返回值
用于声明无初始内容的变量 eg： a=None
------------------------------------------------------------------------------------------------------------------

4.i+=1 #注意python里面没有++运算符
------------------------------------------------------------------------------------------------------------------

5.输入 input  注意了：无论输入的什么类型，获得到的数据永远都是字符串类型
------------------------------------------------------------------------------------------------------------------

6.sorted也是形成新的容器，不对原容器进行修改
------------------------------------------------------------------------------------------------------------------

7.运算符：

逻辑运算符：用于组合条件语句。 Python中的逻辑运算符包括：and、or、not。
与运算符（and）：如果两个条件都为True，则返回True。
或运算符（or）：如果两个条件中至少一个为True，则返回True。
非运算符（not）：如果条件为False，则返回True，否则返回Fals


身份运算符：用于比较两个对象的内存地址。 Python中的身份运算符包括：is、is not
a = 10
b = a
c = 5

print(a is b)       # True
print(a is not c)   # True


------------------------------------------------------------------------------------------------------------------
条件表达式：
值1 if 条件 else 值2