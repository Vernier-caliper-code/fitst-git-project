1.
for z in  range(5,10):
    print(z)

注意这里的z是临时变量，但是这个循环结束之后，还是可以访问z这个变量的，也就是说Python 没有块级作用域（block scope）

2..3
a=in
a=10t(a)  #注意这里一定要用一个变量来接受，int(a)不修改原变量的值
print(a)


3.None的用法
作为函数返回值
用于声明无初始内容的变量 eg： a=None

4.i+=1 #注意python里面没有++运算符

5.输入 input  注意了：无论输入的什么类型，获得到的数据永远都是字符串类型

6.sorted也是形成新的容器，不对原容器进行修改
